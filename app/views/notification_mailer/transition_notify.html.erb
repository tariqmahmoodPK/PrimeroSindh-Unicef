<!DOCTYPE html>
<html lang="<%= @transition&.transitioned_to_user&.locale || I18n.locale %>">
  <head>
    <meta content="text/html; charset=UTF-8" http-equiv="Content-Type" />
  </head>
  <body>
    <p><%= t("email_notification.#{@transition.key}_html",
             user: @transition.transitioned_by,
             agency: @transition&.transitioned_by_user&.agency&.name,
             record_type: t("forms.record_types.#{@transition&.record.class.parent_form}"),
             record_id: link_to(
                 @transition&.record&.short_id, url_for_v2(@transition&.record)
             ),
             service_type: @transition.service).html_safe
       %>
    </p>
    <% if @sig == "no_6389" %>
      <p>This is a regular case.</p>
    <% else %> 
      <p>This is a emergency case.</p>
    <% end %>
  </body>
</html>
